# https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Jira 8.13.X

on:
  push:
  schedule:
    - cron: "0 1 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"

      - name: Download Cache
        uses: actions/cache@v2
        with:
          path: |
            ~/.m2/repository
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-${{ steps.date.outputs.date }}-jira-cache
          restore-keys: |
            ${{ runner.os }}-${{ steps.date.outputs.date }}-jira-cache

      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Build with Maven
        run: mvn -B verify -Darchetype.test.projectsDirectory=target/test-classes/jira-8-13-X
        env:
          MOZ_HEADLESS: 1 # run firefox in headless mode for executing selenium tests
