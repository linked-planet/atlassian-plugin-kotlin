# https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Confluence

on:
  push:
  schedule:
    - cron: "0 1 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"

      - name: Download Maven Cache
        uses: actions/cache@v2
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-${{ steps.date.outputs.date }}-atlassian-plugin-maven
          restore-keys: |
            ${{ runner.os }}-${{ steps.date.outputs.date }}-atlassian-plugin-maven

      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Build with Maven
        run: mvn -B verify -Darchetype.test.projectsDirectory=target/test-classes/confluence
        env:
          MOZ_HEADLESS: 1 # run firefox in headless mode for executing selenium tests
